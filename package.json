{
  "type": "module",
  "dependencies": {
    "dayjs": "^1.11.13",
    "esbuild": "^0.25.6",
    "pdfjs-dist": "^5.3.93"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "^3.6.2",
    "watch": "^1.0.2"
  },
  "scripts": {
    "prebuild": "rm -rf docs && mkdir -p docs/data",
    "build": "esbuild src/main.js --bundle --minify --outfile=docs/main.js",
    "build:dev": "esbuild src/main.js --bundle --outfile=docs/main.js",
    "postbuild": "cp -R data docs/ && cp src/*.html docs && cp src/*.css docs && cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs docs/pdf.worker.mjs",
    "serve": "npm run build && http-server docs",
    "watch": "npm run serve & watch 'npm run build:dev' --interval 0.1 src"
  },
  "prettier": {}
}
